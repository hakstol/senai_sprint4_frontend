<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exercícios</title>

    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>

<body>

    <script>
        let personagens = [{
            nome: "Capitão Cometa",
            dataAparicao: 1951,
            editora: "DC"
        }, {
            nome: "Lanterna Verde",
            dataAparicao: 1940,
            editora: "DC"
        }, {
            nome: "Ciborgue",
            dataAparicao: 1980,
            editora: "DC"
        }, {
            nome: "Homem Aranha",
            dataAparicao: 1962,
            editora: "Marvel"
        }, {
            nome: "Homem de Ferro",
            dataAparicao: 1963,
            editora: "Marvel"
        }, {
            nome: "Capitão América",
            dataAparicao: 1940,
            editora: "Marvel"
        }];

        // 1.   Liste somente os personagens cuja editora seja Marvel;
        console.log("Lista somente os personagens cuja editora seja Marvel")
        let personagensMarvel = personagens.filter(personagens => {
            return personagens.editora === "Marvel"
        })

        console.log(personagensMarvel)

        // 2.	Liste somente os personagem cuja data de Aparição seja superior a 1950;
        console.log("Lista somente os personagem cuja data de Aparição seja superior a 1950")
        let personagensNovos = personagens.filter(personagens => {
            return personagens.dataAparicao > 1950
        })

        console.log(personagensNovos)

        // 3.	Some a idade de todos os personagens;
        console.log("Lista a idade de todos os personagens")
        let idades = personagens.map(personagem => {
            return new Date().getFullYear() -  personagem.dataAparicao
        })
        
        let somaIdades = idades.reduce(function(total,idade){
            return total + idade;
        },0)

        console.log(somaIdades)
        
        // 4.	Traga somente os nomes dos personagens;
        console.log("Lista somente os nomes dos personagens")
        let nomes = personagens.map(personagem => {
            return personagem.nome
        })
        console.log(nomes)

        // 5.	Adicione mais um personagem chamado Capitã Marvel, data de Aparição 1977 e editora Marvel;
        personagens.push(
            {
                nome : "Capitã Marvel",
                dataAparicao : 1977,
                editora : "Marvel"
            })
            console.log(personagens)

        // 6. Mostre a quantidade de personagens da Marvel que possuem na lista
        console.log("Mostrar a quantidade de personagens da Marvel que possuem na lista")
        console.log(personagens.length)

        // 7. Mostre o nome e a idade dos personagens
        console.log("Mostrar o nome e a idade dos personagens")
        let nomeIdades = personagens.map(nomeIdade => {
            return personagens.dataAparicao 
        })
        console.log(nomeIdades)
    </script>

</body>

</html>
